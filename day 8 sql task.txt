use day7_day8_assignment;

create table employees (
    emp_id int primary key,
    emp_name varchar(50),
    salary decimal(10,2),
    department varchar(50)
);

insert into employees values
(1,'ravi',50000,'it'),
(2,'meena',60000,'hr'),
(3,'arun',45000,'it'),
(4,'priya',70000,'hr'),
(5,'kumar',55000,'sales');

select *
from employees
where salary >
(
    select avg(salary)
    from employees
);

create table products (
    product_id int primary key,
    product_name varchar(50),
    price decimal(10,2)
);

insert into products values
(1,'laptop',50000),
(2,'mouse',500),
(3,'keyboard',1000),
(4,'monitor',12000),
(5,'printer',8000);

select *
from products
where price >
(
    select min(amount)
    from orders
);

select *
from customers
where customer_id in
(
    select customer_id
    from orders
);

select customer_name,
(
    select count(*)
    from orders
    where orders.customer_id = customers.customer_id
) as order_count
from customers;

select *
from employees
where salary =
(
    select max(salary)
    from employees employee2
    where employees.department = employee2.department
);

select exists
(
    select *
    from products
    where product_name = 'laptop'
) as product_exists;

select * from employees;
select * from products;